import { PaperWrapper } from "./PaperWrapper";

export const Organizations: React.FC = () => {
    return (
        <PaperWrapper title="Organizations">
            <div>Here is a list of them</div>
        </PaperWrapper>
    );
};
